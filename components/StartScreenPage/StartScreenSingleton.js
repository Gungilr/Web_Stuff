export default class StartScreenSingleton
{
    static instance
    constructor(canvas)
    {
        if(Expirence.instance)
        {
             return Expirence.instance
        }
    }
}